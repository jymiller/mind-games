.env
.env.local
node_modules/
.DS_Store
*.log
scratch/

# private prep — keep untracked
HANDOFF.md

# any .env variant (backups, .local, etc) — keep the template
.env*
!.env.example

# strategy + architecture notes — keep OUT of public history
memory/
INFRASTRUCTURE.md

# runtime attestation register (synthetic, regenerated at will)
data/

# agent config — settings.json is shared, everything else is local
.claude/*
!.claude/settings.json
!.claude/memhub-brains.json
